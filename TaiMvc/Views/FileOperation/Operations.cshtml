@model TaiMvc.Views.FileOperation.Operations

<add namespace="System.Web.Mvc.Ajax" />


<div>
    <h4>Zwykły upload</h4>
    <form action="OperationUpload"
          enctype="multipart/form-data" method="post">
        <input type="file" name="file" />
        <button type="submit">Prześlij</button>
    </form>

    <h4>Upload z hasłem</h4>
    <form action="OperationUploadEncryption"
          enctype="multipart/form-data" method="post">
        <input type="file" name="file" />
        <button type="submit">Prześlij</button>
    </form>

    <h4>Stream Upload</h4>
    <form method="post" enctype="multipart/form-data" action="StreamUpload">
        <input type="file" name="files" multiple />
        <button type="submit">Prześlij</button>
    </form>

    <h4>Stream Encryption Upload</h4>
    <form method="post" enctype="multipart/form-data" action="StreamEncryptionUpload">
        <input type="file" name="files" multiple />
        <button type="submit">Prześlij</button>
    </form>

    <h4>Stream Upload 5.x sample</h4>
    <form method="post" enctype="multipart/form-data" action="UploadLargeFile">
        <input type="file" name="files" multiple />
        <button type="submit">Prześlij</button>
    </form>
</div>
    @*<h4>Stream Upload</h4>
    <form id="uploadForm" action="StreamUpload" method="post"
          enctype="multipart/form-data" onsubmit="AJAXSubmit(this);return false;">
        <dl>
            <dt>
                <label for="file">File</label>
            </dt>
            <dd>
                <input id="file" type="file" name="file" />
            </dd>
        </dl>

        <input class="btn" type="submit" value="Upload" />

        <div style="margin-top:15px">
            <output form="uploadForm" name="result"></output>
        </div>
    </form>
</div>

@section Scripts {
  <script>
    "use strict";
    async function AJAXSubmit (oFormElement) {
      const formData = new FormData(oFormElement);
      try {
        const response = await fetch(oFormElement.action, {
          method: 'POST',
          headers: {
            'RequestVerificationToken': getCookie('RequestVerificationToken')
          },
          body: formData
        });
        oFormElement.elements.namedItem("result").value = 
          'Result: ' + response.status + ' ' + response.statusText;
      } catch (error) {
        console.error('Error:', error);
      }
    }
    function getCookie(name) {
      var value = "; " + document.cookie;
      var parts = value.split("; " + name + "=");
      if (parts.length == 2) return parts.pop().split(";").shift();
    }
  </script>
}*@